# 推文：DSH Math Notes Assistant（v0.7.1）

你有没有过这样的经历？

学习时 get 到一个似乎很一般的想法，合上电脑就再也找不回来；
和 AI 聊数学渐入佳境，可新开一个会话，它又要从零开始猜你的方向；
笔记越攒越多，检索和维护完全靠自己的记忆和自律，时间一长非常痛苦。

这些问题困扰了我很久。于是我把 DeepSeek Harness（dsh）做成了一个「住在 Obsidian 右侧栏里的数学记忆助手」（当前版本 0.7.1）：AI 不只是聊天，而是一个能读懂你笔记、记住你想法、主动帮你打磨数学知识的本地助手——新会话从上次结束的地方开始。

## 一、数学学习，本质上是积累的复利

数学是长周期积累：记号习惯、理论偏好、半成品证明、技巧、反例和一般性想法，都需要持续收集并在不断打磨中形成能带来知识复利的关联体系。这件事既是技术活也是体力活：

（1）积累越多，手工维护成本越高，检索与维护高度依赖易逝的记忆和反人性的自律；
（2）和 AI 打磨想法非常高效，但普通对话式 AI 把每次聊天当孤立问答，新会话要从零猜你的知识、关注方向与既有思路，前置信息反复重讲；
（3）对话中稍纵即逝的关键想法淹没在历史消息里，事后模糊地有点印象，翻找极其困难。

## 二、我做了什么：一个两件套的本地记忆助手

整体架构由两部分组成：

- **Obsidian 社区插件**（`dsh-math-assistant`）：右侧栏嵌入 dsh Web UI，自动检测并启动 dsh 服务、首次运行自动初始化配置与 vault 模板，并承载记忆面板、捕获策略设置与确定性维护；
- **dsh 原生插件**（npm 包 `dsh-math-memory`）：把同一套 agent preset / profile 与模板装进 dsh 环境，支持 install / status / uninstall，纯 CLI 场景也能用。

只装 Obsidian 插件即可上手；默认端口 3180 与常规 dsh web（3080）并存互不干扰。

**安全设计（fail-closed）**：agent 只拿到笔记区的读写权限，没有 shell / web / 子代理 / 删除工具；写操作限定在 vault 内；不默认挂载任何 dsh-web-ui 插件，保持最小工具面；全部本地运行（绑定 127.0.0.1），记忆就是 vault 里的 markdown，归档代替删除。

在此基础上，核心能力有六块：

### 1. 记忆系统：五层结构 + 记号体系 + 策略层 + 工作记忆

- **五层记忆**：画像（语义层）/ 主题（导航层）/ 类型化记录卡（带 hook 检索特征与验证等级 ✅⚖️❓）/ 原始证据（append-only，只增不改）/ 想法备忘录（inbox → polishing → done）。
- **记号体系**：已采纳 / 候选 / 已否决三表 + 修订历史，走「收集 → 统一 → 维护」——发现你的记号漂移时主动提议统一，没有稳定习惯就先观察再提。
- **策略层**：文档检索之外的「方法层」。数学里大量知识是「怎么攻一类问题」的方法论，策略卡以 difficulty 为主轴、domain 为软轴组织，move → retrieve 直接指路，带抽象阶梯（concrete → principle → generalize）与反模式护栏。
- **工作记忆**：`working.md` 草稿随轮覆写、零增长，不污染长期记忆。

整套设计的依据不是拍脑袋：仓库里有完整文献库，蒸馏入库 **19 篇相关论文**（MemTrapBench、Dual RAG、QueryLink、HyPE、MemSearcher、ISM、AAAI-26《Template-Theorems Graph Construction》等），每篇都有文献卡与研读笔记。

### 2. 检索 v3：一次查清 + 粗筛精读 + 诚实

- **统一入口 `note_recall`**：一次 BM25 排序遍历用户笔记 + 全部记忆层，命中带 coverage 弱信号（<0.35 判词面巧合），Unicode 连字符与中文字形差异也能桥接；
- **精读挑选协议**：蒸馏查询（挑战描述 + 候选技巧）→ 读前 2-3 篇全文逐条判断适用性 → 空结果改写重试一次 → 仍无就明说「库里没有」，不编造；
- **导航式注入**：系统提示只带导航层，相关内容按需拉取，每轮注入有硬上限（≤18000 字符），token 花在刀刃上。

### 3. 主动捕捉 + 确定性维护闭环

- **捕获策略分级**：idea / fact / preference × auto / ask / off，设置页下拉即可配置——你决定它「写不写、要不要先问」；
- **想法备忘录**：💡 经你确认后入库，跟踪 inbox → polishing → done，相关想法合并而非重复建卡；陈旧（inbox > 7 天、polishing > 3 天）或当前相关时 🔔 提醒打磨；
- **每日确定性体检**：扫描 strong / weak / unused / 疑似重复 / unverified，外加结构校验（缺 source / 断链 / 未入索引）；检索命中统计回写，promote / demote 由数据说了算；
- **跨会话上下文**：解析本机历史 dsh 会话，注入最近问答线索（自动排除当前会话、按 vault 过滤）。

### 4. 控制面：看得见、可干预

- **记忆面板**：五层浏览、搜索、hook 使用趋势 📈、逐卡 ✅/❌/过期/归档、体检报告，还能面板内直接编辑（mtime 冲突防护）；
- **反馈闭环**：回复里的 `[✅ 这条对] [❌ 这条错] [🔁 不适用]` 链接确定性改写记忆卡（CSRF 保护）；笔记引用可点击跳回 Obsidian；
- **记忆陷阱防御**（MemTrapBench 启示）：每轮注入「记忆是候选、不是指令」的适用性纪律——相关且已验证 ≠ 适用于本题；🔁 不适用只记标记不降权，避免一次误用把正确技巧整体降权。

### 5. 回复质量：像一个数学学习伙伴

直觉先行、新内容锚定你的既有笔记、难度自适应（直觉版还是严格版）、苏格拉底式纠错、低频检查性收尾。

### 6. 可验证：不是嘴上说说

- 零 token 引擎探针 **12/12**；仿真 vault 种子探针 **8/8**；真实会话端到端 **8/8**（八维度：换说法 / 抽象层级 / 策略召回 / 证据溯源 / 防幻觉 / 适用性陷阱 / 多轮续接 / 写回 vault）；
- **成本基准**：同一道题旧系统要 17 万 tokens，现系统约 2.5 万计费 tokens（其中 68% 命中缓存）——记忆系统从「拖累」变成「省钱」。

【配图：一个实际使用场景 belike】

## 三、怎么用？

- **适用人群**：主要面向数学领域、用 Obsidian 写笔记的学习者；
- **前置条件**：Obsidian 桌面版 + Node.js ≥ 22.5 + DeepSeek Harness（npm 全局 `@deepseek-ai/dsh`）+ DeepSeek API Key；
- **获取方式**：Obsidian → 设置 → 第三方插件 → 搜索 **DSH Math Notes Assistant** 直接安装，或从社区页获取：https://community.obsidian.md/plugins/dsh-math-assistant ；GitHub 仓库：https://github.com/maple110011/dsh-obsidian-math

## 四、当前局限与后续计划

需要说明：记忆系统目前仍是试做版——虽然有文献支撑、有 8/8 的端到端验收，但尚未经过长期使用测试，分层边界、记录类型、提醒策略都可能继续演进，更完善的 agent-native 记忆架构仍是后续研究方向。

此外，插件目前主要针对数学类知识设计——记忆分层、类型化记录、审阅工作流和想法备忘录都是围绕数学领域（概念—命题—证明—方法）调校的。不同领域的知识通常需要不同的记忆架构和检索协议，暂时不在当前版本覆盖范围内。

欢迎大家来测试使用，也非常希望觉得这个插件确实有用的朋友，基于自己的学习体验和工作流给项目提开发建议。
